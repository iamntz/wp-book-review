<input type="{{ type}}"
    name="{{{ name }}}"
    id="{{{ id }}}"
    value="{{{ value }}}"
    class="widefat"
    {{#if min}}min="{{{ min }}}"{{/if}}
    {{#if max}}max="{{{ max }}}"{{/if}}
    {{#if step}}step="{{{ step }}}"{{/if}}
>
